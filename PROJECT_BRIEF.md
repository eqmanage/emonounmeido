# えもの運命堂(仮) — プロジェクト仕様書

最終更新: 2026-09-16
サイト名は仮称。正式名称は後日決定。

## 1. サイトの目的

40代を対象に、「このまま今の仕事を20年続けるべきか」という迷いに対して、
これまでの職業経験を新しい可能性へと**翻訳**して見せる、3分程度の可能性診断サイト。

- ターゲット: 40代、今の仕事にこのままでいいのか迷いを感じている層
- 提供価値: 経験の再解釈による、次の一歩のきっかけ
- 収益化: なし(現時点)。情報提供・エンターテインメントが目的

## 2. コンセプト

- 核となる問い: 「このまま20年、働きますか？」
- キーメッセージ: 「あなたの経験を翻訳します。」
- **絶対に外してはいけない条件**:
  - 転職サイトのようなビジネスライク/信頼感重視のトーンにしない
  - 占いサイトのような神秘的・スピリチュアルな世界観にもしない
  - → その中間、「白と青を基調にした、静かで上質な診断体験」を目指す
- ビジュアルモチーフ: **年輪**(積み重ねてきた経験の比喩)
- 星座・誕生日などの占い的要素は、**エンタメ的なフックとして部分的に使用**するが、
  サイト全体を占いとして押し出すことはしない(あくまで職業選択肢を出す手段の一つ)
- **サイト全体の原則**: 裏側で占い・確率計算(星座・干支・数秘術・九星気学・年代性別の傾向など)を
  行っていることは、入力者には一切伝えない。UI上の文言は常に中立的な表現(「近いものがあれば選んでください」等)に留め、
  占術名・スコア・的中率などの内訳を表示しない。結果画面の「生まれ持った気質」カードも例外ではなく、
  見出しは「生まれ持った気質」とだけ表示し、星座占いであることや占術名は一切出さない
  - **例外(2026-09-16追加)**: `about.html`(このサイトについてページ)に限り、監修者紹介の文脈で
    星座・干支・数秘術・九星気学などを組み合わせて診断ロジックを設計していることを明示してよい。
    診断フロー本体(`index.html`)には、この例外は適用しない(引き続き一切開示しない)

## 3. デザイン方針

### カラートークン(css/style.css内 `:root` に定義)

| 変数 | 値 | 用途 |
|---|---|---|
| `--navy` | #16233F | 見出し、ボタン、主要テキスト |
| `--navy-2` | #2B3F63 | サブアクセント |
| `--sky` | #EFF4F9 | 淡い青の背景 |
| `--sky-2` | #DCE6F0 | カード背景・境界線 |
| `--paper` | #FCFBF8 | メイン背景(暖かみのある白) |
| `--ink` | #23262E | 本文テキスト |
| `--muted` | #6E7787 | 補足テキスト |
| `--line` | #D9E1EA | 罫線 |

白と青のみで構成し、金色・朱色など占い的な差し色は使わない。

### タイポグラフィ

- 見出し: `Zen Old Mincho`(和文明朝体、編集的で上質な印象)。ウェイトは**900(最太)**を使用し、
  力強さ・信頼感を演出(40-50代は細すぎる書体だと読みにくく、頼りない印象になりやすいため)
- 本文: `Noto Sans JP`、ウェイト400(通常)
- 基準フォントサイズは `html { font-size: 106.25%; }` で全体を底上げ(実質17px相当)。
  ターゲット層の可読性を優先し、標準的な16pxよりやや大きめに設定

### 信頼感の演出

- ランディングのCTAボタン直下に「登録不要・入力内容は保存されません」の一言(`.trust-note`)を明示。
  40-50代は診断・占い系サイトへの警戒心が強いため、早い段階で安心材料を見せる

### トンマナ

- 角丸カード・派手なグラデーションは避け、罫線ベースのシンプルな構成
- 装飾は年輪(同心円)のSVGのみ。星やタロットなど西洋占い的モチーフは使わない
- `<meta name="color-scheme" content="light only">` と CSS `color-scheme: light only;` を設定し、
  端末のダークモード設定による自動反転(文字色・背景色のコントラスト崩れ)を防止。
  このサイトは常にライトモード配色で表示される

## 4. サイト構成・UXフロー

単一の `index.html` 内で、JSによる画面切り替え(SPA的な構成)。複数ページには分割していない。

1. **ランディング**: 「このまま20年、働きますか？」→「自分の可能性を診断する(3分・無料)」
2. **STEP1「あなたのことを教えて」**: 性別 / 誕生日
3. **STEP2「今のことを、少しだけ」**: 今のお仕事 / 最近の気持ち(5択・間接的な聞き方)
4. **STEP3「最後にひとつだけ」**: ちょっとやってみたい仕事
5. **結果画面**: 経験の翻訳結果を表示

`about.html`(このサイトについて)のみ、周辺ページとして用意している(index.htmlのミニヘッダーからリンク)。
2026-09-16、監修者紹介コンテンツを追加(5-10参照)。お問い合わせ窓口は現時点では設けていない。

## 5. 機能仕様

### 5-1. 星座判定ロジック

STEP1の誕生日から12星座を算出(`getZodiac()`)。一般的な西洋占星術の日付区分を使用。

誕生日入力(`<input type="date">`)のデフォルト値は **1980年4月2日**。
ターゲット層(40代・50代)の年齢に合わせつつ、厚生労働省「人口動態統計」
(1981〜2015年、テレビ番組でも紹介された集計)で最多とされる日付を採用している。
入力可能範囲は1940〜2010年(`min`/`max`)。

### 5-2. STEP2「今のお仕事を教えてください」— 複合占いによる職業候補の提示

質問文は「今のお仕事を教えてください」。自由入力ではなく、**複合占いのスコアが高い上位8つの職業候補+その他(自分で入力)** をボタンで提示する(`renderJobChoices()` / `getTopJobCandidates()`)。
投票で8件に満たない場合は、`allJobCategories`(全カテゴリ一覧)から不足分を補充し、常に8件になるようにしている。

候補は以下4つの占術による投票制で算出する(`computeJobScores()`):

| 占術 | 算出元 | 関数/辞書 |
|---|---|---|
| 星座×デカン | 誕生日(月日) | `zodiacJobHints`(重み3,2,1) |
| 干支 | 誕生日(年) | `getEto()` / `etoJobHints`(重み2,1) |
| 数秘術(運命数) | 誕生日の数字を1桁に還元 | `getNumerology()` / `numerologyJobHints`(重み2,1) |
| 九星気学(本命星) | 誕生日(年、節分2/4で年切り替え) | `getKyuseiNumber()` / `kyuseiJobHints`(重み2,1) |
| 年代・性別の緩やかな傾向 | 年齢層×性別 | `getStatsHint()`(重み1,1、実際の統計データの引用ではなく占いの一要素として軽く加味) |

各占術が職業カテゴリに投票し、合計スコア上位3件を候補として表示する。

- **性別は、この職業候補の算出(年代・性別の傾向枠)にのみ使用**。他の占術(星座・干支・数秘術・九星気学)には性別を使わない
- **UI上の表示方針**: 裏側で複数の占術を使って計算していることは、入力者には伝えない。
  「近いものがあれば選んでください」という中立的な表現に留め、占術の名称やスコア・的中率は一切表示しない
- 候補にないものは「その他(自分で入力する)」で自由入力可能
- (旧仕様として「まず1件を予想し、当たっているか確認する」方式や、的中率の表示、星座×デカンのみで候補を出す方式も検討したが、現在は上記の「複合スコア上位3件を直接提示」に統一している)

### 5-3. STEP3「やってみたい仕事」の選択肢生成

**8択**に拡張(`renderWishChoices()`):
- Q4「気持ち」ごとの、より具体的な方向性の候補 × 4(`feelingDreamJobs`)
- 星座ごとの「気になる分野」候補 × 4(`zodiacDreamJobs`、STEP2とは異なる、より夢のある職業リスト)
- 上記8つに加えて「その他(自分で入力する)」「特にない」

Q4の回答を明確に反映させることで、「今の気持ち」と「やってみたい仕事」のつながりを強めている。

- **UI上の表示方針(STEP2と同様)**: 星座や気持ちの回答をもとに算出していることは、入力者には伝えない。
  質問文・補足テキストともに「気になるものがあれば選んでください」という中立的な表現に留める

### 5-4. 職業翻訳辞書(`occupationDictionary`)

STEP2の職業回答をキーワードマッチングし、以下を返す:
- `essence`: その職業経験の本質を一言で表現
- `suggestions`: 新しい職業候補の**プール(6件)**、それぞれに
  - `reason`: 橋渡しの理由(一般論)
  - `episode`: より具体的な行動ベースのエピソード
  - `peopleFacing`: 人と接する仕事かどうか(レーダーチャートに使用)
  - `tag`: どの「気持ち」(Q4)に響きやすいかを示すラベル(`continue` / `unrewarded` / `repetition` / `other-desire` / `vague` / `neutral`のいずれか)
- `uniqueSuggestion`: より意外性のある1つの職業候補(下記5-4-1参照)

現在**17カテゴリ**を収録: エンジニア・IT / 経理・総務・人事 / 営業 / 販売・接客 /
運送・配達・タクシー / 工場勤務・製造 / 飲食店勤務 / 土木・建設 / 医療・介護 / 公務員 /
保育士 / 教師 / 主婦・主夫 / 美容師 / **経営者・自営業** / **芸能・エンターテインメント** /
**アスリート・スポーツ関連**(太字は2026-09追加分)。

**方針**: 「公務員・サラリーマン・経営者・芸能・医療・アスリート」のような大きな枠へ
全面差し替えする案も検討したが、**あえて見送った**。「サラリーマン」のような
大きすぎる枠は、ほぼ全員が該当してしまい、職業翻訳の結果が抽象的になって
サイトの核となる価値(具体的で意外性のある翻訳)を損なうため。代わりに、
公務員・医療は既存カテゴリでカバーしつつ、経営者・芸能・アスリートの3つを
**専用の翻訳ロジックを持つ独立カテゴリとして追加**し、粒度を保ったまま対応範囲を広げた。
「サラリーマン」はあえて独立カテゴリ化せず、既存の営業・経理総務人事・エンジニア等の
細分化されたカテゴリで表現される前提とした。

未該当の場合は `genericTranslation` にフォールバック(こちらも6件のプールと`uniqueSuggestion`を持つ)。

#### 5-4-2. プールからの重み付き選択(`selectSuggestions()`)

6件のプールから、結果画面に表示する4件を選ぶ際に:
- 入力内容(職業・気持ち・星座・やってみたい仕事)をシードにした決定的な擬似ランダム値を各候補に付与
- **Q4で選んだ「気持ち」の`tag`と一致する候補に大きめのボーナス**を加算し、優先的に選ばれやすくする
- `tag: 'neutral'` の候補にも小さめのボーナスを加算し、常に一定の露出を確保
- スコア上位3件(気持ちに合う・無難な提案)を採用し、**残り3件から1件を「あえての意外な提案」として必ず混ぜる**(4件目)。
  無難な提案だけで結果が埋まらないよう、意図的に外れ値を1つ含める設計

同じ回答内容であれば毎回同じ4件が選ばれる(決定的)。適合度スコア(`matchScores()`)は
この選ばれた4件に、順に80-94% / 62-79% / 48-61% / 35-47%を割り当てる。

#### 5-4-1. 意外性のある提案(`uniqueSuggestion`)

各カテゴリが持つ、通常の6件プールとは別枠の「もう一歩踏み込んだ意外な提案」。
点線ボーダーのカードで区別し、「適合度◯◯%」ではなく「意外性のある提案」というラベルを表示する
(スコアは付けない)。**全ユーザーに表示**する(旧仕様では「提示した候補通りに職業を選んだ人」限定
だったが、2026-09の調整で全員表示に変更し、驚きの精度・頻度を底上げした)。

### 5-5. マッチ度(適合度%)

`job + 気持ち + 星座 + やってみたい仕事` を結合した文字列をシードにハッシュ化し、
決定的に算出(`matchScores()`)。同じ回答なら毎回同じ数値になる。
- 1つ目の提案: 80〜94%
- 2つ目の提案: 62〜79%

### 5-6. 傾向レーダーチャート

**8項目・4対の構成**(`computeTraitAxes()`)。それぞれ完全に独立させるのではなく、
対になる概念のペアとして設計し直した:

| 軸 | 対 |
|---|---|
| 挑戦志向 | 安定志向 |
| リーダーシップ性 | 職人気質(専門性追求) |
| 人との関わり | 単独集中力 |
| 論理的思考力 | 感受性・共感力 |

内部的には `challenge` / `leadership` / `connection` / `logic` の4つの核スコア(0-100)だけを
星座・気持ち・職業から算出し、対極の4項目(`stability` / `craft` / `solo` / `empathy`)は
それぞれ `100 - 核スコア` で自動的に導出する。円周上は各ペアが真反対(180度)になるよう配置。

スコアの算出元:
- 星座ごとの4核スコアのベース値(`zodiacTraitBase`)
- 気持ち(Q4)の回答による補正(`feelingAdjust`)
- 職業が人と接するタイプかどうか(`peopleFacing`) — 「人との関わり/単独集中力」のみ補正

SVGで動的に描画(`renderRadarSVG()`)。8方向に均等配置し、ラベルは各軸の角度方向に
沿って外側に配置、水平位置に応じて`text-anchor`を自動切り替えることで、viewBox内に
収まるようにしている。表示サイズは最大420px、ラベルのフォントサイズは15(viewBox内座標)。
2026-09-16、「小さくて見えづらい」というフィードバックを受け、グラフ本体の半径(`maxR`)を
90→100、ラベルまでの余白(`labelGap`)を20→16に調整し、かつCSS側の表示上限を380px→420pxに
拡大。ラベルがviewBox(440×440)からはみ出さないことをPlaywrightでの描画確認済み。

### 5-7. シェア機能・OGP

- 結果画面に「Xでシェア」「LINEで送る」ボタン
- サイト本体(`index.html`)の静的なOGPタグ(`og:title` / `og:description` / `og:image` / `twitter:card`)は、
  クローラーがトップページを直接読む場合のフォールバックとして残置(OG画像は`img/og-image.png`、共通の1枚)
- **2026-09、結果ごとに異なるOGP画像を出す仕組みを実装済み**: GitHub Pages自体は静的なままで、
  別リポジトリ・別サービス(Vercel: `emonounmeido-ogp`)を追加する構成を採用
  - シェアボタンのリンク先は、診断ページ自身ではなく`https://emonounmeido-ogp.vercel.app/api/share?job=...&score=...`
    (Vercel Edge Functionの中継ページ)に向けている(`js/app.js`の`runDiagnosis()`内)
  - `/api/share` が、職種名・適合度を埋め込んだ`og:image`等のmetaタグを含む静的HTMLを返す(クローラー向け)。
    人がクリックした場合は、画像と「自分も診断してみる」ボタン(本サイトへのリンク)を表示
  - `/api/og` が、`@vercel/og`(Satori)を使って1200×630のOGP画像をその場で動的生成
    (職種名・適合度をクエリパラメータで受け取り、ブランドトークンに沿ったデザインで描画)
  - リポジトリ: `eqmanage/emonounmeido-ogp`(このリポジトリとは別管理)

### 5-8. 性別の扱い(方針)

- **性別は、キャリア(職業)の実質的な提案内容の決定要因にはしない**という方針
- 使用箇所は2つ:
  1. 結果画面の「生まれ持った気質(星座占い)」カード(`zodiacFortune`) — 星座ごとの気質説明(共通)に、性別に応じた一言を追加する程度
  2. STEP2の職業予想(`getStatsHint()`) — 年代×性別の緩やかな傾向を、複数ある占術投票の1つとして軽く加味(重みは他の占術と同程度〜やや低め)
- 「回答しない」を選んだ場合、1は共通説明のみ、2は該当の傾向枠なしで計算される
- 職業の翻訳ロジック(`occupationDictionary`)・マッチ度・レーダーチャートには性別を使用しない
- この方針は意図的なもの: 性別によってキャリアの選択肢を強く決めつけることを避けるため

### 5-9. 「生まれ持った気質」カードへの複合占い統合

当初は星座のみだったが、STEP2/3の裏側で計算している干支・数秘術・九星気学も、
この結果カードに反映するよう拡張(`getFortuneText()`)。

- 星座の気質説明(`zodiacFortune`の`base`)に続けて、干支・数秘術・九星気学それぞれの
  一言気質(`etoTraits` / `numerologyTraits` / `kyuseiTraits`)を1文にまとめて追加し、
  最後に性別由来の一言を続ける、という4段構成
- **文章量の方針**: 星座のみだった頃の1.5倍程度を上限とし、簡潔な体言止め寄りの表現でまとめる
- **トーンの方針**: この「気質」カードに限っては、他セクション(職業翻訳など)よりも
  断定的・自信のある言い回しを使い、信頼感を演出する(例:「〜傾向があります」ではなく
  「〜表れます」「〜つながります」といった言い切り表現を優先)。これは占いカードだけの
  例外的なトーン設定であり、キャリア提案部分の「余白を残す言い回し」の方針(7章)とは
  意図的に区別している
- **表示テキストに占術名・干支の名称を出さない**: サイト全体の原則(2章)に則り、
  「子年」「数秘術7」「六白金星」のような占術名・数字・干支の名称は文章中に一切表示しない。
  各占術から得た「気質の中身」だけを地の文に溶け込ませ、何を根拠に算出しているかは開示しない
  (例: 「また、機転の利く社交性や探求心、誇り高いリーダー気質も併せ持っています。」)

### 5-10. Aboutページの監修者コンテンツ(2026-09-16追加)

`about.html`に、サイトの成り立ちの説明に加えて「監修者について」セクションを追加。

- 監修者名: えも
- 資格表記: 「占術カウンセラー(仮称)」— **仮の名称**。正式な資格名が決まり次第、
  `about.html`内の`.supervisor-cred`要素を差し替える必要がある
- プロフィール: カリフォルニアでMBA取得後、米国公認会計士(CPA)として大手会計コンサルで実務経験。
  帰国後、日本国内のベンチャー〜大手企業でキャリアを重ね、占術と統計学の両方に精通
- スタイル: `.supervisor-card`(罫線+薄い青背景のカード)、`.supervisor-cred`(バッジ風の資格表示)を
  `css/style.css`に追加。既存の`.suggestion-card`/`.suggestion-score`と統一感のあるトーンにしている
- 2章の「占い計算を入力者に一切伝えない」原則に対する例外として、このページに限り
  診断ロジックが星座・干支・数秘術・九星気学を組み合わせていることを明示している(詳細は2章参照)

### 5-11. 提案カードの「もっと詳しく知る」パネル(2026-09-16、試作中)

結果画面の提案カードに、任意で開閉できる詳細パネルを追加できる仕組みを実装。

- 各`suggestions`の要素に、`detail: { points: [...], links: [{ title, url }, ...] }`を
  任意で追加できる(`points`=根拠を深掘りした箇条書き、`links`=参考記事へのリンク)
- `detail`を持つ提案にのみ「もっと詳しく知る」ボタンが表示される(`renderSuggestionDetail()`)。
  ボタンを押すとパネルが開閉する(`result-body`へのイベント委譲で実装、`js/app.js`)
- **参考記事リンクの方針**: 実在しない「特定個人の成功事例インタビュー」を作り話で用意することは避け、
  Web検索で実際に見つかった、信頼できる解説記事のみを厳選して掲載する。見出しも「成功事例」ではなく
  「この仕事についてもっと知る」としている
- **現在の試作範囲**: 「営業」カテゴリの2件(`独立系の営業代行・コンサルタント`、`営業研修・OJTトレーナー`)
  にのみ`detail`を実装済み。デザイン・文章トーンの確認後、他のカテゴリへの展開を検討する
- `points`の文章は、既存の`reason`/`episode`と同様、占い的な根拠(星座・干支等)には一切触れず、
  スキルの転用可能性など論理的な観点のみで構成している

- 完全な静的サイト。バックエンドなし、データ保存なし
- 構成ファイル:
  ```
  index.html
  about.html
  css/style.css
  js/app.js
  img/og-image.png
  ```
- ホスティング: GitHub Pages を想定(リポジトリ: `emonounmeido`。旧名 `eminounmeido` から改名)
- 依存ライブラリなし(Vanilla JS)。Webフォントのみ Google Fonts から読み込み
- 結果ごとの動的OGP画像生成のみ、別リポジトリ`emonounmeido-ogp`(Vercel Edge Functions)を併用(5-7章参照)

## 7. コンテンツトーン・ガイドライン

- 断定を避け、「〜かもしれません」「〜という道もあります」のような余白を残す言い回し
- 統計的根拠がない旨、結果画面下部に明記(エンターテインメント目的の注記)
- 経済的・診断的な冷たさを避け、手紙のような温度感のある文章にする
- ネガティブな感情(Q4の選択肢)には、否定せず受け止める一言を必ず添える

## 8. 今後の検討事項(未決定)

- サイト名の正式決定
- 「得意なこと」「優先したいこと」など追加設問の是非(結果の精度向上 vs 3分体験の維持)
- お問い合わせ窓口の要否・方式(メールアドレス掲載 / 外部フォーム / 設けない、のいずれか未決定)
- ユーザーデータの保存要否(現時点では不要の方針)
- `about.html`の監修者資格表記「占術カウンセラー(仮称)」の正式名称確定(5-10参照)

### 解決済み(参考)

- 診断結果ごとに異なるOGP画像を出すためのサーバーサイド対応 → 2026-09、
  別サービス(Vercel: `emonounmeido-ogp`)による動的OGP画像生成を実装済み(5-7章参照)
- Aboutページの追加 → 既存(`about.html`)。2026-09-16、監修者紹介コンテンツを追加(5-10章参照)

## 9. 集客方針(参考メモ)

- 無料: シェア機能起点の拡散、SNS投稿、SEO記事、既存コミュニティでの紹介
- 有料: 検索連動型広告、SNS広告(年齢ターゲティング)、ネイティブ広告、インフルエンサー起用
- 1日1000人規模を狙う場合、無料施策だけでは「バズり」に依存する部分が大きく、
  広告は立ち上げ初期の起爆剤として短期集中投下し、口コミ・シェアで後押しする設計を推奨
